// #include "bin/inc/user.hpp"
#include "bin/inc/format.hpp"

#include <iostream>

int main()
{
    // makeLine('=', 10, 20, 30);
    makeLine('-', 10, 20, 30);
    printLine('|', "Hello", 10, "World", 20);
    // printLine("HELLO", 10, "World", 20);
    // int choice;
    // accounts::AccountHolder holder;

    // while ((choice = initializer()) && choice % 3)
    //     holder = interactions.at(choice)();

    // std::cout << "Quiting...";
    // return 0;
}